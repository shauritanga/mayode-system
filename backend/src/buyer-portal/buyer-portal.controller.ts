import { Controller, Get, Param, UseGuards } from '@nestjs/common'; import { UserRole } from '@prisma/client'; import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard'; import { RolesGuard } from '../auth/guards/roles.guard'; import { Roles } from '../auth/decorators/roles.decorator'; import { BuyerPortalService } from './buyer-portal.service';
@UseGuards(JwtAuthGuard, RolesGuard) @Roles(UserRole.BUYER, UserRole.ADMIN, UserRole.SUPER_ADMIN) @Controller('buyer-portal') export class BuyerPortalController { constructor(private readonly portal: BuyerPortalService) {} @Get('profile') profile() { return this.portal.profile(); } @Get('dashboard') dashboard() { return this.portal.dashboard(); } @Get('traceability/:reference') trace(@Param('reference') reference: string) { return this.portal.traceability(reference); } }
