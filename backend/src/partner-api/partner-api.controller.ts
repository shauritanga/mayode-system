import { Body, Controller, Get, Param, Post, Req, UseGuards } from '@nestjs/common'; import { ApiBearerAuth, ApiTags } from '@nestjs/swagger'; import { UserRole } from '@prisma/client'; import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard'; import { RolesGuard } from '../auth/guards/roles.guard'; import { Roles } from '../auth/decorators/roles.decorator'; import { PartnerApiGuard } from './partner-api.guard'; import { PartnerApiService } from './partner-api.service';
@ApiTags('partner-api') @Controller('partner') export class PartnerApiController { constructor(private readonly partners: PartnerApiService) {} @Post('keys') @ApiBearerAuth() @UseGuards(JwtAuthGuard, RolesGuard) @Roles(UserRole.SUPER_ADMIN) key(@Body('partnerName') partnerName: string) { return this.partners.createKey(partnerName); } @Get('v1/farmers/:id/credit-profile') @UseGuards(PartnerApiGuard) profile(@Param('id') id: string, @Req() req: any) { return this.partners.creditProfile(req.partnerApiKey.id, id, req.ip); } }
