/** Automatic role templates are intentionally disabled.
 * Super Admin creates every non-system role and its permissions in Role Management.
 */
export async function seedRoleTemplates(_prisma: unknown): Promise<void> {}
